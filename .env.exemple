# Exemple d'environnement local — NE PAS y mettre de vraie clé dans le dépôt.
# Copier vers `.env` (gitignored) :
#
#   OPENAI_API_KEY=sk-...
#
# Aucune valeur par défaut. Absente → fournisseur réel indisponible (fail closed).

OPENAI_API_KEY=
